// The install one-liner. A literal shell command (not localizable copy) shown
// in the landing hero/footer, copied by the hero's copy button, and surfaced in
// the device screen's "install wharf" modal.
export const INSTALL_COMMAND = "curl -fsSL wharf.sh/install | sh";
